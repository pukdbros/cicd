apiVersion: v1
kind: Service
metadata:
  name: simple-boilerplate-app-mee-service-dev
  namespace: group-4
spec:
  selector:
    app: simple-boilerplate-app-mee-deployment-dev
  ports:
  - port: 80
    # protocol: TCP
    targetPort: 3000
  type: ClusterIP